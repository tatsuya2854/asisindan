# 美脚の魔女図鑑 — 作業メモ

インフルエンサー「なるねぇ」の依頼で作った脚の診断アプリ。
インスタとYouTubeで宣伝され、商品（チェンミーのほぐほぐクリーム）に繋ぐのが目的。

## 構成

- `index.html` と `leg-witch.html` は**中身が同じ**。片方を直したら必ずコピーする。
  （旧版が index.html に残っていて古いアプリが公開される事故が実際に起きた）
- 依存ライブラリはゼロ。CDNも使わない。QRエンコーダも自前。
  → **外部ライブラリを足さないこと。** 落ちた日にシェアカードが壊れる。
- ES5で書かれている（var、関数式）。合わせること。

## 公開先

**Cloudflare Workers（プロジェクト名 bikyakusindan）。**
main に push すると自動デプロイ。GitHub Pages は使っていない。

- 本番 https://bikyakusindan.toropicanafanta.workers.dev/
- `og:url` / `og:image` / `twitter:image` / `canonical` の4か所に絶対URLが直書き。
  ドメインを変えるときは index.html と leg-witch.html の両方を置換する。

## テスト

```
# ローカル配信（本番と同じくルート直下に置く）
mkdir -p /tmp/root && cp -r index.html ogp.jpg app-icon.png manifest.webmanifest img /tmp/root/
npx http-server /tmp/root -p 8792 -c-1
```

`window.__witch` にデバッグ用フックがある（result / rarity / buildCard / events / setLine 等）。

## 踏んだ地雷（同じ穴に落ちないこと）

1. **実機の画面高さは 664px 前後。テストの800pxで通っても実機で壊れる。**
   `.sheet-in` は縦flexで、flexアイテムは既定で縮む。カードが押し潰されて
   overflow:hidden で下半分が消えていた。小画面（iPhone SE=568px）で必ず確認する。

2. **`var` の巻き上げ。** `buildCard` 内で座標に `var ty` を使い、上位スコープの
   `ty`（タイプ情報）を潰してカード生成が全落ちした。既存の変数名を確認してから使う。

3. **CSSの子孫セレクタの効き範囲。** `.linecard .lmark` と書いたせいで、
   商品カード側の同じ要素に背景が当たらず白飛びした。使い回す部品は単独クラスにする。

4. **`<u>` の入れ子。** `stars()` は空の星を `<u>` で出す。それを外側から
   `<u>` で包んで金色CSSを当てたら、全員★5に見えた。レア度は嘘をつくと致命的。

5. **span は inline。** `.thumb` に `width:100%` と `position:relative` を書いても
   効かず、「あなた」バッジが高さ4pxに潰れていた。`display:block` を明示する。

6. **ホスティング先を確認せずにOGPの絶対URLを決めない。**
   GitHub Pages 前提で書いたが本番は Cloudflare で、本番のOGPが壊れた。
   コミットの check-runs を見れば何が動いているか分かる。

7. **「動いている」と「そう見える」は別。**
   魔法陣を7秒で1回転させていたが、絵が左右対称なので回転が伝わらず、
   中央の「?」だけが傾いて壊れて見えていた。計測では動いていた。

## 検証の型（これは効いた）

- **QRは実デコーダ（jsQR）で読めることを確認してから組み込む。** 見た目では判断しない。
  書き出したPNGのピクセルからデコードして16タイプ全部通した。
- **回帰テストを毎回流してから push する。** `reg` スクリプトで21件。
- **実機スクショを見てもらう。** 今回のバグ4件は実機でしか出なかった。

## 計測

GA4（`G-N1FTJ40NZJ`）。**GA4側での設定作業は不要にしてある。**

- 設問到達は `lw_q01`〜`lw_q12` と名前を分けてある。
  パラメータにするとカスタムディメンション登録が要るので、名前で分けた。
- 流入元は `utm_source` を使う。GA4が標準で解釈するので設定不要。
  `src=` も後方互換で読む。

**人に手作業を頼む設計になったら、まず作業自体を消せないか考えること。**

## 触るときの注意

- `AD_URL` / `LINE_URL` / `AD_LINE_URL` は空にするとその枠だけ消える。
- `AD_EVERY = 0` で商品カード非表示。
- 商品カードの見出しは `ADBRIDGE` に16タイプ分ある。ほぐす場所と対応しているので、
  `hp`（ほぐほぐ1POINT）を変えたらこちらも合わせる。
- 結果画面の「ほぐほぐ1POINT → 商品カード」は点線で繋がった1本の導線。
  **間に何か挟まない。** 図鑑の進捗バーを挟んで導線を切った前科がある。
